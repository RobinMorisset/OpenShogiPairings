#!/bin/sh
# Slower gate that only needs to run before sharing work: the full test suite.
set -e

echo "== cargo test --workspace =="
cargo test --workspace

echo "== npm test (frontend) =="
(cd frontend && npm test)
