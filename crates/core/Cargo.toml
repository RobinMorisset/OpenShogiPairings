[package]
name = "osp-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
integer-blossom = { workspace = true }
serde = { workspace = true }
uuid = { workspace = true }
thiserror = { workspace = true }
ts-rs = { workspace = true }
derive_more = { workspace = true }
typed-index-collections = { workspace = true }
rand = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
# Test-only: the seeded RNG the sim tests replay a run with. Every use is inside
# a `#[cfg(test)]` module, so as a normal dependency it was compiled into the
# server and desktop builds for nothing.
rand_chacha = { workspace = true }
